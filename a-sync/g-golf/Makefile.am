## Process this file with automake to produce Makefile.in

SOURCES = base.scm await-ports.scm meeting.scm

moduledir = $(GUILE30_SITEDIR)/a-sync/g-golf
module_DATA = $(SOURCES)

EXTRA_DIST = $(SOURCES)
